# ollama discord midleware

this is a midleware that creates a discord bot that responds to `/grok` and `@grok` in a discord server and forwards the prompt to ollama and returnes the ollama response stream in the discord server
